const DATABASE_ERROR_MESSAGES = {
  UNIQUE_CONSTRAINT_VIOLATION: "Unique constraint violation.",
  INVALID_INPUT_SYNTAX: "Invalid input syntax.",
  DATABASE_EXCEPTION: "Database exception.",
} as const;

export { DATABASE_ERROR_MESSAGES };
